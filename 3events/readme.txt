the index.html  file contains a form and a list that can be interacted with to add names to list. it also allos the user to clear the list and generate a random
winner from the list.

the code.js handles adding names to the list by creating a new li element to the list shell. the code.js also handles clearing the list
generating a winner and manipulating the winner text.
