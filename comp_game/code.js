
/*

var form = document.getElementById("myForm");










document.addEventListener("DOMContentLoaded", function() {
  form.addEventListener("submit", function(event) {
    // prevent the default form submission behavior
    event.preventDefault();
  
    userN = form.username.value;
    userWep = form.Weapons.value;
    userH_D = form.Health/Damage.value;
    userP = form.TotalPoints.value;
  
    console.log(userN, userWep, userH_D, userP);
  
  
  });
});


function statBuilder(charBuilder){
  var userN = document.getElementById("username");
  var userWep = document.getElementById("Weapons");
  var userH_D = document.getElementById("Health/Damage");
  var userP = document.getElementById("TotalPoints");
  
  
  
  
  
  userN = charBuilder.username.value;
  userWep = charBuilder.Weapons.value;
  userH_D = charBuilder.Health/Damage.value;
  userP = charBuilder.TotalPoints.value;
 
  console.log(userN, userWep, userH_D, userP);


}
 */