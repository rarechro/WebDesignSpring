the index.html file is used to give the user  slideshow, when the user presses the button the image is changed the the next position in an array and set the the value of that pos.

the code.js file contains an array of images that are incremented through when the button on index.html is pressed.

styles.css changes the backround color to make it a bit mor appealing.